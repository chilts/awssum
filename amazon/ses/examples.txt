name    : SendEmail
title   : Send a simple email
intro   : This example shows how to send an e-mail without an attachment. Ensure you have verified your send e-mail address (and receive e-amil address if you are not in production mode).
example : <<EOF
var params = {
    ToAddresses    : ['example@domain.com'],
    CcAddresses    : ['email@domain.com'],
    BccAddresses   : ['contact@domain.com'],
    Text           : 'This is an e-mail to test SES.\n\nThis is another line of text.',
    Html           : '<p>This is an e-mail to test SES.<br />This is another line of text',
    Subject        : 'Example SES mail',
    Source         : 'mail@anotherdomain.com',
};
ses.SendEmail(params, function(err, data){
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SendEmail
title   : Send a simple email with specific charsets.
intro   : This example shows how to send an e-mail with a specific charset. Ensure you have verified your send e-mail address (and receive e-amil address if you are not in production mode).
example : <<EOF
var params = {
    ToAddresses    : ['example@domain.com'],
    Text           : 'This is an e-mail to test SES with a smiley ☺.\n\nThis is another line of text.',
    TextCharset    : 'UTF-8',
    Html           : '<p>This is an e-mail to test SES with a smiley ☺.<br />This is another line of text',
    HtmlCharset    : 'UTF-8',
    Subject        : 'Example SES mail',
    SubjectCharset : 'UTF-8',
    Source         : 'mail@anotherdomain.com',
};
ses.SendEmail(params, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SendRawEmail
title   : Send a simple email with specific charsets.
intro   : This example shows how to send an e-mail with a specific charset. You can add all kinds of files, but read the supported filetypes by AWS SES and use the appropriate filetypes. Use BASE_64 as the encoding for the string which starts below with FD87697... Ensure you have verified your send e-mail address (and receive e-amil address if you are not in production mode).
example : <<EOF
var message = '';
message     += "To: example@domain.com\n";
message     += "From: mail@anotherdomain.com\n";
message     += "Subject: Example SES mail (raw)\n";
message     += "MIME-Version: 1.0\n";
message     += 'Content-Type: multipart/mixed; boundary="aRandomString_with_signs_or_9879497q8w7r8number\n"';
message     += "\n";
message     += "--aRandomString_with_signs_or_9879497q8w7r8number\n";
message     += 'Content-Type: text/plain; charset="utf-8"\n';
message     += "Content-Transfer-Encoding: 7bit\n";
message     += "Content-Disposition: inline\n";
message     += "\n";
message     += "Dear new tester,\n\n";
message     += "Attached is the file you requested.\n";
message     += "\n\n";
message     += "--aRandomString_with_signs_or_9879497q8w7r8number\n";
message     += "Content-ID: \<77987_SOME_WEIRD_TOKEN_BUT_UNIQUE_SO_SOMETIMES_A_@domain.com_IS_ADDED\>\n";
message     += 'Content-Type: application/pdf; name="somename.pdf"\n';
message     += "Content-Transfer-Encoding: base64\n";
message     += 'Content-Disposition: attachment; filename="somename.pdf"\n';
message     += "\n";
message     += "FD87697F8SDF986S78DF6S8F7DG67FS6G987FS6DG9876FDS87G6FSD789G70897SVDFS90887S9D7G7FDS6FG9DSF7G9FDS77FG8";
message     += "\n";
message     += "--aRandomString_with_signs_or_9879497q8w7r8number--\n";

ses.SendRawEmail({ RawMessage : message }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : ListVerifiedEmailAddresses
title   : List all verified email addresses
intro   : This example shows how to list all verified email addresses that need to be verified before you can sent emails from those email addresses.
example : <<EOF
ses.ListVerifiedEmailAddresses({}, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : ListIdentities
title   : List all identities 
intro   : This example shows how to list all identies (email addresses and domains) regardless of its verification status.
example : <<EOF
ses.ListIdentities({}, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetSendQuota
title   : Get the quota status on the amount of emails sent
intro   : This example shows how to show the status on how many emails are sent and are yet possible to be sent.
example : <<EOF
ses.GetSendQuota({}, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetSendStatistics
title   : Get the send statistics
intro   : This example shows how to show the statistics of all email traffic for the last 2 weeks as a list of data points.
example : <<EOF
ses.GetSendStatistics({}, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : DeleteIdentity
title   : Delete an identity
intro   : This example shows how to delete an identity (e-mail or domain) regardless if it is verified or not. It replaces SES.DeleteVerifiedEmailAddress.
example : <<EOF
ses.DeleteIdentity({ Identity : 'email@domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
ses.DeleteIdentity({ Identity : 'domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});

EOF
---
name    : DeleteVerifiedEmailAddress
title   : (deprecated) Delete a verified email address
intro   : This example shows how to delete a verified email address only. Notice the difference with SES.DeleteIdentity that is used for unverified email addresses and/or domains.
example : <<EOF
ses.DeleteVerifiedEmailAddress({ EmailAddress : 'email@domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : VerifyEmailAddress
title   : (deprecated) Verify an email adress 
intro   : This example shows how to verify an email address. Afterwards an email will be sent to the specified email adress where you have to click on a link to verify its existence.
example : <<EOF
ses.VerifyEmailAddress({ EmailAddress : 'anotheremail@domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : VerifyEmailIdentity
title   : Verify an email adress 
intro   : This example shows how to verify an email address. Afterwards an email will be sent to the specified email adress where you have to click on a link to verify its existence. It replaces SES.VerifyEmailAddress.
example : <<EOF
ses.VerifyEmailIdentity({ EmailAddress : 'anotheremail@domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : VerifyDomainIdentity
title   : Verify a domain
intro   : This example shows how to verify a domain. Please visit <a href="http://docs.amazonwebservices.com/ses/latest/DeveloperGuide/InitialSetup.DomainVerification.html">here</a> for more information.
example : <<EOF
ses.VerifyDomainIdentity({ Domain : 'domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : VerifyDomainDkim
title   : Verify a domain DKIM entry
intro   : This example shows how to verify a domain DKIM entry in its DNS. Please visit <a href="http://docs.amazonwebservices.com/ses/latest/DeveloperGuide/DKIM.EasyDKIM.DNSRecords.html">here</a> for more information.
example : <<EOF
ses.VerifyDomainDkim({ Domain : 'domain.com' }, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetIdentityDkimAttributes
title   : Get identity DKIM attributes
intro   : This example shows how to get identity DKIM attributes to verify its DNS settings.
example : <<EOF
var params = {
    Identities : [ 'email@domain.com', 'domain.com' ],
};
ses.GetIdentityDkimAttributes(params, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetIdentityNotificationAttributes
title   : Get identity notification attributes
intro   : This example shows how to get identity notification attributes for bounce and complaints notifications.
example : <<EOF
var params = {
    Identities : [ 'email@domain.com', 'domain.com' ],
};
ses.GetIdentityNotificationAttributes(params, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetIdentityNotificationAttributes
title   : Get identity notification attributes
intro   : This example shows how to get identity notification attributes for bounce and complaints notifications.
example : <<EOF
var params = {
    Identities : [ 'email@domain.com', 'domain.com' ],
};
ses.GetIdentityNotificationAttributes(params, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : GetIdentityVerificationAttributes
title   : Get identity verification attributes
intro   : This example shows how to get identity verification attributes for verification status/tokens.
example : <<EOF
var params = {
    Identities : [ 'email@domain.com', 'domain.com' ],
};
ses.GetIdentityNotificationAttributes(params, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SetIdentityDkimEnabled
title   : Set identity DKIM to enabled/disabled
intro   : This example shows how to enable/disable DKIM on a identity (domain/email address).
example : <<EOF
var params1 = {
    Identity    : 'email@domain.com',
    DkimEnabled : true,
};
ses.SetIdentityDkimEnabled(params1, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});

var params2 = {
    Identity    : 'email@domain.com',
    DkimEnabled : false,
};
ses.SetIdentityDkimEnabled(params2, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SetIdentityFeedbackForwardingEnabled
title   : Set identity feedback forwarding to enabled/disabled
intro   : This example shows how to enable/disable feedback forwarding on a identity (domain/email address). It is important for bounce and complaint notifications.
example : <<EOF
var params1 = {
    Identity          : 'email@domain.com',
    ForwardingEnabled : true,
};
ses.SetIdentityFeedbackForwardingEnabled(params1, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});

var params1 = {
    Identity          : 'email@domain.com',
    ForwardingEnabled : false,
};
ses.SetIdentityFeedbackForwardingEnabled(params2, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SetIdentityNotificationTopic
title   : Set an identity notification (SNS) topic
intro   : This example shows how to set the notification (SNS) topic per identity to publish bounce and complaint notifications to.
example : <<EOF
var bounce = {
    Identity         : 'email@domain.com',
    NotificationType : 'Bounce',
    SnsTopic         : 'arn:aws:sns:us-east-1:123456789012:exampleTopic',
};
ses.SetIdentityNotificationTopic(bounce, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});

var complaint = {
    Identity         : 'domain.com',
    NotificationType : 'Complaint',
    SnsTopic         : 'arn:aws:sns:us-east-1:123456789012:exampleTopic'
};
ses.SetIdentityNotificationTopic(complaint, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
name    : SetIdentityNotificationTopic
title   : Delete an identity notification (SNS) topic
intro   : This example shows how to delete the notification (SNS) topic per identity to publish bounce and complaint notifications to.
example : <<EOF
var noBounce = {
    Identity         : 'email@domain.com',
    NotificationType : 'Bounce',
    SnsTopic         : '',
};
ses.SetIdentityNotificationTopic(noBounce, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});

var noComplaint = {
    Identity         : 'domain.com',
    NotificationType : 'Complaint',
    SnsTopic         : '',
};
ses.SetIdentityNotificationTopic(noComplaint, function(err, data) {
    fmt.dump(err, 'Error');
    fmt.dump(data, 'Data');
});
EOF
---
